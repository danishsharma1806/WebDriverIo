/*describe("interaction with web element",function()
{
    it("Enter value in the field",function()
    {
        browser.url('/');
        const search=$('#twotabsearchtextbox');
        search.setValue('Iphone 11');
        browser.pause(5000);
        const label=$('span.a-size-base.a-color-base');
        let text=label.getText();
        console.log(text);
    });

    it("Click on field",function()
    {
        browser.url('/');
        const icon=$('input.nav-input.nav-progressive-attribute');
        icon.click();
    });
})*/